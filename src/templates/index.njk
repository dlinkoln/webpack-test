<!doctype html>
<html>
  <head>
    <title>Getting Started</title>
  </head>
  <script>
    
  </script>
  <body>
    {% block name%}
        <p>{{john}}</p>
    {% endblock %}
    
    {% for item in items %}
        <li>{{ item.title }}</li>
    {% else %}
        <li>This would display if the 'item' collection were empty</li>
    {% endfor %}
  </body>
  <script>
  </script>
</html>